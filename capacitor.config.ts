import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.playweb.tankautomation',
  appName: 'TankAutomation',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
